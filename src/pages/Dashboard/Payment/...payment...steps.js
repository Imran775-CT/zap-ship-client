/**
 * 1.  which payment gateway -- (we choose stripe);
 * 2.  format a payment form (make a form where we can get the form data)
 *
 * 3.validate card information .
 * 4. create a payment intent in the server.
 * 5. confirm payment is successful : update payment status on the parcel also enter an entry in the parcel history collection
 *
 *
 * notes: we used stripe js ,,,
 *
 */
